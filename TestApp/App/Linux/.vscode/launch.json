{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/testapp",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}